# =============================================================================
# MFEE - Manga File Converter Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Backend API Configuration (Required)
# -----------------------------------------------------------------------------
# The URL of the backend API server
# - For development: http://localhost:8060
# - For production: Your deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:8060

# Server-side API URL (optional, falls back to NEXT_PUBLIC_API_URL)
# Use this if your server needs a different URL to reach the backend
API_BASE_URL=

# -----------------------------------------------------------------------------
# Clerk Authentication (Optional)
# -----------------------------------------------------------------------------
# Get your keys at: https://dashboard.clerk.com/last-active?path=api-keys
# If not provided, the app will run without authentication features

# Public key for client-side authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Secret key for server-side authentication
CLERK_SECRET_KEY=

# -----------------------------------------------------------------------------
# Upload & Conversion Settings (Optional)
# -----------------------------------------------------------------------------
# Maximum number of files that can be uploaded at once
# Default: 10
NEXT_PUBLIC_MAX_FILES=10

# Maximum concurrent multipart upload parts
# Default: 3
NEXT_PUBLIC_MAX_CONCURRENT_PARTS=3

# Global maximum concurrent parts across all uploads
# Default: 8
NEXT_PUBLIC_GLOBAL_MAX_CONCURRENT_PARTS=8

# Upload timeout in milliseconds for individual parts
# Default: 180000 (3 minutes) - 300000 (5 minutes) based on upload speed
# Increase this for slower or unstable connections
# Example: 600000 = 10 minutes
NEXT_PUBLIC_UPLOAD_TIMEOUT_MS=300000

# Time in seconds before a queued job is considered for processing
# Default: 5
NEXT_PUBLIC_QUEUED_SECONDS=5

# Time-to-live for converted files in days
# If set, displays how long files will be available after conversion
NEXT_PUBLIC_TTL=7

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV=development
